# 변경 이력

모든 버전의 요약은 `metadata.txt`의 changelog에도 있습니다. 여기가 상세본입니다.

## 0.1.4 (2026-09-22)

**주제: 실용성과 정직성.** 실제 QGIS 3.34에서 수정 전후를 비교해 검증한 세 차례 감사 라운드의 결과입니다. 기능을 제거한 항목은 없습니다. 자세한 근거는 `docs/REGRESSION_REPORT.md`와 `docs/AUDIT_BACKLOG.md`에 있습니다.

### 사용자에게 보이는 변화
- 모든 도구의 도움말 끝에 **학술 근거와 권장 절차** 블록을 추가했습니다(구현한 연구, 저자의 의도, 권장 순서, 주장하지 않는 것, 원문).
- 모든 대화상자가 마지막 입력값을 기억합니다(출력 경로와 키 계열 제외).
- 아이콘을 `icons/`로 모으고 256 px로 줄였습니다(플러그인 크기 8.7 MB 감소).
- 히구치 거리대 경계를 조정할 수 있습니다(기본 500/2500 m 유지).
- 가시성 네트워크의 LOS에 지구 곡률·굴절 보정이 기본 적용됩니다(끌 수 있음).
- 상대 경사 비용(구 "Conolly & Lake")의 기준 경사 기본값이 1°에서 5°로 바뀌었습니다.
- **QGIS 4(Qt6/PyQt6) 대비**: `qgisMaximumVersion`을 4.99로 올렸습니다. PyQt6가 없앤 표기(스코프 없는 열거형, `QVariant.Type`, `exec_()`)와 QGIS 4가 지운 3.x 열거형(`QgsWkbTypes.PointGeometry`, `QgsMapLayerProxyModel.Filter`, `QgsRasterBandStats.Stats`, `QgsUnitTypes.Distance*` 등)을 소스 전체에서 양쪽에 통하는 표기로 바꿨고, 버전에 따라 이름이 다른 몇 가지는 `tools/qtcompat.py`가 실행 중인 QGIS를 조사해 고릅니다. QGIS 3.34에서는 결과가 수치까지 같고(회귀 27건), PyQt6 6.11에서 소스가 쓰는 모든 Qt 이름의 존재를 확인했습니다. **QGIS 4 빌드에서 실제로 실행해 보지는 못했습니다**(`docs/PUBLISHING.md` 6절).
- 플러그인 저장소 업로드용 ZIP 빌드(`scripts/build_plugin_zip.py --check`), 태그 푸시 시 ZIP을 첨부하는 릴리스 워크플로, 업로드 절차 문서(`docs/PUBLISHING.md`).
- 원본 1024 px 아이콘은 `docs/art/`에 보존합니다.
- 아이콘 원본 15장 중 12장의 우측 하단에 이미지 생성기가 남긴 네 꼭짓점 별 마크가 있었습니다. `scripts/strip_corner_mark.py`(OpenCV 쉬프트맵 인페인팅, 평면 영역은 색 채우기)로 지우고 256 px 아이콘을 다시 만들었습니다. 바뀐 픽셀은 우측 하단 모서리뿐이며, 마크가 없던 3장(등고선·경사도·도면 시각화)은 그대로입니다.
- README의 도구 표와 `docs/TOOLS.md`의 도구 제목에 QGIS 메뉴에 보이는 아이콘을 붙였습니다. 거리 래스터와 지질도 ZIP은 전용 아이콘이 없어 각각 비용표면·지구화학도 아이콘을 함께 씁니다.
- 변수 상관/VIF 리포트와 트렌치 후보 제안에 전용 아이콘을 그렸습니다(`scripts/make_tool_icons.py`, 기존 세트의 프레임·캡션 양식을 따른 픽셀아트). 메뉴·대화상자·README·TOOLS.md가 새 아이콘을 씁니다.

### 결과가 달라지는 수정
- 나머지 도구 13개 코드 리뷰 라운드(결함마다 합성 데이터로 재현한 뒤 수정, 재현은 `tests/test_*_review2.py`). 공통: 결과 그룹을 맨 위로 옮기며 그룹을 지워 이전 결과가 사라지던 문제를 5개 도구(AHP, 지적도, 지구화학도, 경사도 도면화, 지형 단면)에서 고치고 `tests/test_layer_tree_group_move.py`로 막음. 권한 코드 없는 좌표계를 출력 레이어에 유지. CI의 QGIS 작업을 최소 지원 버전 3.40과 LTR에서 Processing 포함으로 실행.
- 최소비용 네트워크: 같은 DEM 셀의 유적 쌍을 직선·평지 비용 간선으로 연결(SNA 누락 수정), 권한 코드 없는 DEM CRS 유지, 대칭화 선택을 모든 방식에 표시·메타데이터 기록, k-NN k가 후보 k보다 크면 후보 k 자동 상향, All 모드 SNA가 합집합 그래프임을 명시, NULL 이름은 fid로, 점 여러 개인 멀티포인트는 실행 거부.
- 근접/가시성 네트워크: LOS 고도를 양선형 보간으로 읽고 관측·대상 셀을 제외해 경사지 오판정 수정(경사면 45쌍 중 1-5쌍 → 45쌍), 폴리곤 경계 가시성이 실제로 계산되도록 수정, Gabriel 엄격 정의, 샘플 간격 0 = 픽셀, 2곳·일직선 유적 경로 처리, 같은 위치 유적 연결, 권한 코드 없는 CRS 유지, NULL 이름 fid.
- 비용표면: 차량/수레 임계경사를 %로 해석(Herzog 2013, 기본 12%, 최소비용 네트워크도 동일), 실행 중 창 닫기 후 버튼 비활성 수정, 권한 코드 없는 CRS에서 경로·점·마일스톤 생성, 마찰 벡터의 레이어 필터·메모리 레이어 지원, 마일스톤·프로파일·판돌프 시간에 마찰 반영, 모든 모델 변수를 메타데이터에 기록.
- 지적도 중첩: 재실행 시 결과 그룹 삭제 수정, 기하 모음 교차에서 행이 사라지던 문제 수정, 이전 결과 필드 재사용 시 새 값 기록, 권한 코드 없는 CRS 유지, 분할 모드 합계 중복 제거, 면적 방식 기록. 지질도 ZIP: NULL·공백 코드를 클래스로 굽지 않고 '값 없음'으로 집계, CRS 없는 도엽 병합 거부, .cpg 인코딩 존중, 같은 이름 ZIP 파일 보존, NoData와 코드 충돌 방지, 같은 ZIP 재로드 시 교체.
- 지구화학도: 재실행 시 결과 그룹 삭제 수정, 범례 경계값 색은 그 값에서 시작하는 구간으로 분류, 경계선 보간은 무채색 선과 안티앨리어싱 가장자리만 최근접 값 복사로 메움(흰 배경 등은 NoData 유지, 중간 구간 없음), 회색 무자료 구간 NoData 처리는 범례 첫 색이 회색일 때만, 범례 이미지의 연속 색상바/상자형 구분, 고농도 스냅 t<0.5 수정, NoData 폴리곤 옵션 동작, 투명 픽셀은 불일치 경고에서 제외, 분석 범위 밖 구역 보고, 픽셀 크기 소수 6자리.
- 지형 분석: 반경 2셀 이상 TPI를 정확한 초점평균으로 계산(이전 근사는 곡면에서 약 3배 과대), 사면방향의 평탄은 -1·NoData는 -9999로 구분하고 정북은 N으로, TPI 레이어가 자동 SD 임계값을 실제 사용·표기, 비정사각 픽셀 곡률, Weiss 분류명 Valley/Ridge, TPI·TRI·거칠기 단위 m. 경사도/사면방향 도면화: 재실행 시 결과 그룹 삭제 수정, 경사 단계가 구역·라벨을 실제로 조절하고 반올림 전에 구간 판정.
- DEM 생성: 등고선과 표고점(또는 첫 개체 유형이 다른 DXF 여러 장)을 함께 보간, DXF 개체 순서와 무관하게 점은 점·선은 구조선으로, 도 단위 좌표계와 Z가 모두 0인 3D 자료 거부, TIN과 크리깅이 같은 필드·같은 편집 상태를 사용, 픽셀 0 거부, 스냅 반올림 오차 보정, IDW 설정 기록.
- 가시권: DEM 밖/NoData 관측점은 단일 모드에서 거부하고 다중·선형·둘레 모드에서는 제외 후 개수 보고, 반경 밖과 DEM NoData 셀은 '보이지 않음'이 아니라 NoData이며 결과 래스터는 DEM 범위를 넘지 않음, 누적 마스크 셀 중심 비교, 폐곡선 시작점 중복 제거, 반복 실행 시 결과 레이어를 관측점으로 자동 선택하지 않음, LOS는 픽셀 간격 표본·끝점 검사·경로 NoData는 판정 불가·표시 고도는 DEM 값, AOI 통계는 셀 중심·결과값>0 기준, 역방향 메타데이터 높이 정정, 분석 반경 링 생성, 최대 거리 최소 1 m. 반경 밖 판정은 GDAL의 -ov 표시에 기대지 않고 관측 셀 중심에서 잰 거리로 도구가 직접 계산합니다(GDAL 3.11은 출력 창의 첫·마지막 행에서 반경 밖 셀을 0으로 써서 QGIS 3.44 LTR에서 가짜 '보이지 않음' 띠가 생겼습니다).
- 지형 단면: 재실행 시 단면 그룹 삭제 수정, 그래프는 원시 샘플(평활화는 선택)·수직 과장 배율 표시, NoData 구간에서 선을 끊고 상승·경사 통계에서 제외, AOI 구간 길이는 실제 교차로, 좌표계 변경 뒤에도 단면선 저장 정상, 원래 DEM 없는 저장 단면은 다시 계산 전에 확인. 도면 시각화: 그레이 배경을 DEM 최소-최대로 늘임, 코드 이름을 국토지리정보원 코드표(연속수치지형도 설명서 Ver 5.1.1, 지형지물 표준코드)에 맞추고 건물 코드 B0014114, B0014116-B0014119 추가, 건물 면 생성 수정, 고도 범례에 구간값.
- 도구 6개 코드 리뷰 라운드(각 결함을 합성 데이터로 재현한 뒤 수정): 거리 래스터의 최대 거리 밖 셀에 NoData 값을 기록(이전에는 -9999가 거리값으로 남음)하고 미터 아닌 좌표계를 거부. 정렬/내보내기의 AOI 자르기를 기준 격자에 정렬하고 레이어 지정 NoData를 리샘플링에서 제외하며 CRS 없는 소스를 거부. 상관/VIF의 AOI를 폴리곤 내부 표본으로 바꾸고 표본 수를 셀 수로 제한하며 취소 시 리포트를 만들지 않음. AHP의 단일값 기준·범위 밖 선호값·0-100 색상표·래스터 밖 AOI·AOI 변경 후 통계 재계산·계층 일관성비율 표시를 고침. 트렌치의 발자국 최대 경사를 실제 사각형 안 셀로 측정하고 AHP NoData 후보와 DEM 없는 후보를 제외해 집계. AI 조사요약의 캐시 무효화, NULL 이름, 내부 유적 서술, 방향 분포, 선택 레이어 처리, 무효 기하 복구·집계, 범주형 래스터 히스토그램, 버퍼 기준 표기를 고치고 Gemini 클라이언트의 PyQt6 호환(헤더 상수, 오류 판정)을 수정. Qt6 검사기가 QtNetwork 이름과 정수 열거형 인자도 잡도록 보강. 각 항목은 `tests/test_*_review.py`가 재현·검증합니다.
- 지형 분석: 사용자 반경 TPI의 창을 (2r+1)셀로 교정. 작은 DEM에서는 3x3으로 대체하고 알림.
- DEM 생성: 2D 등고선에 표고 필드가 없으면 실행 거부(이전에는 전부 NoData인 DEM을 "완료"로 보고). 격자를 픽셀 정수배로 스냅. Kriging (Lite)가 측점에서 입력값을 정확히 재현.
- 지구화학도: 범례 색과 먼 픽셀은 NoData(허용오차 RGB 48). 구역 통계를 픽셀 중심 규칙으로 변경하고 `zone_area`, `fill_pct`, `burn_rule`, `area_unit` 필드 추가. 최댓값 보정은 AOI 마스크 안에서 계산.
- 지질도 ZIP: 코드표를 도엽 전체 정렬 순으로 통일. 셀 초기값 NoData. CSV에 BOM, `cell_count`, `labels_all`.
- 근접 네트워크: 좌표가 같은 유적이 간선을 공유(이전에는 고립 노드).
- 가시성 네트워크: DEM NoData 샘플은 "검사 불가"(`fail_deg`)이지 "안 보임"이 아님. `betw_norm` 추가.
- 트렌치 제안: `rank`는 점수순, `pick_order`는 배치 순서. 발자국 최대 경사 검사와 `slope_max_deg`. 매장 유형 어휘 14종과 총/릉 규칙. xlsx 범례. 참고 유적 거리를 반경 안에서 정확히 측정.
- 거리 래스터: 선·면을 닿는 셀 모두에 굽기(`EXTRA=-at`). 대상 셀이 없거나 비정사각 픽셀이면 거부.
- 정렬/내보내기: 사면방향을 최근접으로 리샘플링. 메타데이터 없는 래스터는 `categorical=unknown`. 변수 키의 대소문자 충돌 방지. 사용자 지정 CRS를 `SOURCE_CRS`로 전달. 유효 픽셀 0이면 실패, `valid_pct` 기록.
- AHP: 계층 모드에서 전역가중치를 실제로 사용하고 `weights_source` 기록. 표를 손으로 고치면 계층 모드 해제.
- AI 조사요약: 참고 유적을 전수 분류한 뒤 최근접 선택. 잘린 Gemini 응답 표시. 스캔·표본·레이어 상한을 컨텍스트·CSV·프롬프트에 기록. 길이·면적을 m/m²로 변환.

### 정직성
- 인용 73건을 Crossref·Math-Net.Ru·law.go.kr로 검증. 존재하지 않는 문헌 0건, 오귀속 9건 정정(한국표준 경사 구분은 국토지리정보원이 아니라 산림청 예규, Higuchi 1975/1983 판본 분리, TPI 지수 출처는 Weiss 2001 등).
- REFERENCES.md를 (A) 호출 알고리즘 / (B) 직접 구현 / (C) 맥락 참고로 재구성. Wheatley 1995, Malczewski 2004, O'Brien 2007 추가(검증됨).
- 플러그인이 정한 등급 구간은 "플러그인 정의"로 표기.

### 검증
- QGIS 의존 테스트를 포함한 350건이 실제 QGIS 3.34에서 통과. CI에 QGIS 컨테이너 작업 추가.
- `tests/test_qt6_compat.py`: QGIS 3 전용 표기 금지(항상), PyQt5에서 스코프 없는 열거형 0건, PyQt6에서 소스가 쓰는 Qt 이름 전부 존재(CI static-checks가 PyQt6를 설치해 실행).
- `tests/regression/`: 수정 전후 비교 하네스(27개 시나리오). 비교 과정에서 결함 2건 발견·수정.

## 0.1.3

- 보안 스캔 대응: 삼킨 예외 472건 전부 `[swallowed]` 로그 기록, 로깅 싱크 재귀 방지, 회귀 테스트.
- 전체 코드 리뷰 후 수정: 도움말 크래시, AHP 채점 모드·NoData 전파·격자 검증, 트렌치 커버리지 우선 배치, 등고선 NoData, Pandolf 하강 클램프, 네트워크 closeness 보정(Wasserman-Faust), 가시권 샘플링·곡률·굴절, 지형 분석 메모리·CRS 가드, 정렬/내보내기 원자적 게시와 범주형 보호, 거리 래스터 도구 신설, TRI 반경, Weiss 분류의 TPI 반경 반영, 비용 분석 메모리 예산, AHP 가이드 문서.

## 0.1.2

- 긴 텍스트 스크롤 팝업, flake8 정리.

## 0.1.1

- UI 언어 설정, 공용 대화상자 크롬, i18n 회귀 수정, 지질 QML 파싱 안전화.

## 0.1.0

- 첫 공개 베타: 지형, 가시권, 비용, 네트워크, 지질, 지구화학, 지적 중첩, 스타일링, AOI 보고.
